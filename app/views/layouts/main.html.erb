<!DOCTYPE html>
<html>
  <head>
    <title>PrideFriends</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <div class="container">
    <div class="row">
      <div class="col">
        <%= image_tag('pride_friends.png') %>
      </div>
      <div class="col" id="content">  
        <!-- <p class="notice"><%= notice %></p>
         <p class="alert"><%= alert %></p> -->
        <%= yield %>
      </div>
    </div>
  <div class="nav-bar">
    <% if current_user %>
    <%= link_to 'back', :back, class: "button", id: "left-button" %>
    <%= button_to "log out", destroy_user_session_path, :method => "delete", :class => "button" %>
    <% else %>
     <%= button_to "log in", user_session_path, :method => "get", :class => "button" %>
    <% end %>
  </div>  
</div>
  </body>
</html>
